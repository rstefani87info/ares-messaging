# Panoramica CLI — @ares/messaging

## Comandi CLI

Questo modulo **non espone alcun comando CLI** (nessun `bin` nel `package.json`).

## npm scripts

```json
{
  "test": "echo \"Error: no test specified\" && exit 1"
}
```

### `test`
- **Scopo:** Placeholder — nessun test implementato.

## Note

- Modulo pensato per essere importato come libreria
- Le classi `Channel`, `Message`, `Topic` vengono usate programmaticamente
