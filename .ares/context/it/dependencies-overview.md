# Panoramica dipendenze — @ares/messaging

## Dipendenze @ares (workspace)

### @ares/scd
- **Dipendenza di sviluppo**
- Controllo del ciclo di sviluppo

## Dipendenze esterne

Nessuna dipendenza runtime esterna dichiarata nel `package.json`. Il modulo è autocontenuto.

## Moduli @ares che dipendono da questo modulo

Non è possibile determinare automaticamente quali moduli dipendano da `@ares/messaging` analizzando solo questo package. La dipendenza va verificata nei `package.json` degli altri moduli del monorepo.
